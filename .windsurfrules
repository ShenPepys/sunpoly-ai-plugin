
## 全局规则

### 代码编写第一原则
- **人类可读性优先**：生成的代码必须让人类通俗易懂。宁可多写几行清晰的代码，也不要把逻辑压缩成一行难以阅读的长表达式。复杂表达式应拆分为带有明确含义的临时变量，每一步做什么一目了然。

### 语言与沟通
- **对话语言**：全程使用中文进行交流
- **代码注释**：所有代码注释必须使用中文，要清晰简洁,避免重述显见内容，不要仅描述代码功能，要说明实现原因各具体实现方式
- **术语解释**：对专业术语提供简要中文解释

### 文件与操作管理
- **文件创建**：禁止创建 progress.txt、notes.md 等不必要的中间文件，除非任务明确要求
- **批量修改**：批量修改文件前必须获得用户明确确认，并注意处理字符编码问题
- **文件提交**：代码编写完成后不自动提交，需等待用户手动执行提交操作
- **删除操作**：执行任何删除操作前必须明确警告并获得确认

### 变更影响控制
- **不影响其他功能**：修改代码时必须确保不影响其他已有功能，修改前需评估影响范围，如有潜在影响必须提前告知用户

### 代码与工程规范
- **JSDoc 3 规范写注释**：使用JSDoc 3规范写注释
- **日志管理**：使用日志管理工具，如 Serilog，或工程中自定义的日志管理工具
- **代码模块化**：代码要模块化，功能要分明，避免过大的文件，如果单个文件超过1200行，需要拆分成更小的模块
- **拆分文件命名**：拆分后的文件必须以 `原文件名_a_功能说明`、`原文件名_b_功能说明`、`原文件名_c_功能说明` 依次命名，保留原文件名前缀
- **拆分前备份**：拆分文件前必须先备份源文件（如 `原文件名 copy.js`），备份文件不自动删除，由人工检查功能正常后手动删除
- **重构前备份**：重构核心业务逻辑文件前，同样必须先备份源文件（如 `原文件名 copy.js`），备份文件不自动删除，由人工检查功能正常后手动删除
- **代码完整性**：提供完整、可运行的代码示例
- **错误处理**：关键代码需包含必要的错误处理机制
- **依赖说明**：明确标注所需的依赖包和版本
- **环境兼容**：考虑不同操作系统和运行环境的差异

### 安全与隐私
- **敏感信息**：示例中不使用真实密码、API 密钥等敏感信息

### 项目协作规则
- **格式规范**：遵循项目既定的代码风格和目录结构
- **文档同步**：代码修改在获取用户确定后，同步更新相关文档（README.md、workplan.md 等）
- **测试要求**：根据项目需要询问是否添加测试用例

### 内容质量要求
- **分步解释**：复杂操作需要提供分步指导和解释