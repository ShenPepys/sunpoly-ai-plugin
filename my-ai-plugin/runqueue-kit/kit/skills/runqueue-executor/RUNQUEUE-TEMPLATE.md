# <项目名> 执行队列

> 自动化只处理本文件**第一个** `- [ ]` 项。背景见 [<源文档>.md](./<源文档>.md)。
>
> **队列约定**
> - **自动检查**：Hook 续跑唯一依据
> - **手动检查**：不阻塞自动化
> - **验收标准**：只写机器可判断的条目

启用：「**按 RUNQUEUE 执行**，遵循 runqueue-executor skill」。

---

## 已完成

（已从源文档落地、无需再做的项，`- [x]` 归档）

---

## 队列（按顺序执行，勿跳项）

### [<ID>] <标题>

- [ ] 状态：待做

**目标**

<一句话>

**范围**

- 主要：`<路径>`
- 不改：`<边界>`

**验收标准**

- <机器可断言的条目>

**自动检查**

```bash
<单行或多行 shell；Hook 按行执行，python -c 必须单行>
```

**手动检查**

- <可选 UI/联调步骤>

**提交信息模板**

`<type>(<scope>): <message> (<ID>)`

---

## 已移出本队列

| ID | 项 | 说明 |
|----|-----|------|
| | | 需人工拍板 / 跨仓库 / 未定义自动检查 |
